<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:integration="http://www.springframework.org/schema/integration"
	xmlns:jms="http://www.springframework.org/schema/jms"
	xmlns:jee="http://www.springframework.org/schema/jee"
	xmlns:core="http://activemq.apache.org/schema/core"
	xmlns:batch="http://www.springframework.org/schema/batch"
	xmlns:task="http://www.springframework.org/schema/task"
	xmlns:lang="http://www.springframework.org/schema/lang"
    xmlns:int-amqp="http://www.springframework.org/schema/integration/amqp"
    xmlns:p="http://www.springframework.org/schema/p"
    xmlns:rabbit="http://www.springframework.org/schema/rabbit"

    xsi:schemaLocation="http://www.springframework.org/schema/jee http://www.springframework.org/schema/jee/spring-jee-3.0.xsd
        http://www.springframework.org/schema/integration/amqp http://www.springframework.org/schema/integration/amqp/spring-integration-amqp.xsd
		http://www.springframework.org/schema/lang http://www.springframework.org/schema/lang/spring-lang-3.0.xsd
		http://www.springframework.org/schema/jms http://www.springframework.org/schema/jms/spring-jms-3.0.xsd
		http://activemq.apache.org/schema/core http://activemq.apache.org/schema/core
		http://www.springframework.org/schema/task http://www.springframework.org/schema/task/spring-task-3.0.xsd
		http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/integration http://www.springframework.org/schema/integration/spring-integration.xsd
		http://www.springframework.org/schema/batch http://www.springframework.org/schema/batch/spring-batch.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-3.0.xsd
		http://www.springframework.org/schema/rabbit http://www.springframework.org/schema/rabbit/spring-rabbit.xsd">

	<!--bean id="connectionFactory" class="org.apache.activemq.ActiveMQConnectionFactory" >
		<property name="brokerURL">
			<value>tcp://localhost:61616</value>
		</property>
	</bean-->

    <rabbit:connection-factory id="connectionFactory" />

    <rabbit:template id="amqpTemplate" connection-factory="connectionFactory" />


    <!-- Slave request messages begin here -->
 
 	<!--int-jms:inbound-channel-adapter id="jmsRequests" connection-factory="connectionFactory" destination-name="requests" channel="requests" >
		<integration:poller fixed-delay="500"></integration:poller>
	</int-jms:inbound-channel-adapter-->

    <int-amqp:inbound-channel-adapter channel="requests" queue-names="batch.requests" connection-factory="connectionFactory" />

	<integration:channel id="requests"/>
	
	<integration:service-activator id="srvActivator" input-channel="requests" output-channel="replies" ref="chunkHandler" method="handleChunk"/>

	<integration:channel id="replies">
		  <integration:interceptors>
	        <integration:wire-tap channel="loggingChannel"/>
	    </integration:interceptors>
	</integration:channel>
	
	<!--int-jms:outbound-channel-adapter id="outgoingReplies" connection-factory="connectionFactory" destination-name="replies" channel="replies"/-->
    <int-amqp:outbound-channel-adapter channel="replies" amqp-template="amqpTemplate" exchange-name="batch.test.exchange" routing-key="batch.replies"/>


    <integration:logging-channel-adapter id="loggingChannel" level="DEBUG" log-full-message="true" logger-name="slavelogger"/>

    <rabbit:queue name="batch.requests" />
    <rabbit:queue name="batch.replies" />

    <rabbit:direct-exchange name="batch.test.exchange">
        <rabbit:bindings>
            <rabbit:binding queue="batch.requests" key="batch.requests" />
            <rabbit:binding queue="batch.replies" key="batch.replies" />
        </rabbit:bindings>
    </rabbit:direct-exchange>

</beans>
