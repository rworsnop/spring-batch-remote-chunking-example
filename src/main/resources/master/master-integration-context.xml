<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans" 
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:beans="http://www.springframework.org/schema/beans" 
	xmlns:integration="http://www.springframework.org/schema/integration"
	xmlns:batch="http://www.springframework.org/schema/batch" 
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:task="http://www.springframework.org/schema/task"
    xmlns:p="http://www.springframework.org/schema/p"
    xmlns:int-amqp="http://www.springframework.org/schema/integration/amqp"
     xmlns:rabbit="http://www.springframework.org/schema/rabbit"

    xsi:schemaLocation="http://www.springframework.org/schema/integration http://www.springframework.org/schema/integration/spring-integration.xsd
		http://www.springframework.org/schema/task http://www.springframework.org/schema/task/spring-task-3.0.xsd
		http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-2.5.xsd
		http://www.springframework.org/schema/batch http://www.springframework.org/schema/batch/spring-batch-2.1.xsd
		http://www.springframework.org/schema/integration/amqp http://www.springframework.org/schema/integration/amqp/spring-integration-amqp.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-3.0.xsd
		http://www.springframework.org/schema/rabbit http://www.springframework.org/schema/rabbit/spring-rabbit.xsd">



    <rabbit:connection-factory id="connectionFactory" />

    <rabbit:template id="amqpTemplate" connection-factory="connectionFactory" exchange="batch.test.exchange"/>

    <!-- Remote Chunking Requests (The Producing of Chunks) Integration Configuration -->

 	<bean id="messagingGateway" class="org.springframework.integration.core.MessagingTemplate">
		<property name="defaultChannel" ref="requests" />
		<property name="receiveTimeout" value="5000" />
	</bean>


	<!-- Remote Chunking Replies From Slave -->


    <integration:logging-channel-adapter id="loggingChannel" level="DEBUG" log-full-message="true" logger-name="masterloggerIn"/>
    <integration:logging-channel-adapter id="loggingOutChannel" level="DEBUG" log-full-message="true" logger-name="masterloggerOut"/>

	<!--task:executor id="poolExecutor" pool-size="5-10" queue-capacity="0" /-->

    <integration:channel id="replies">
        <integration:queue/>
        <integration:interceptors>
            <integration:wire-tap channel="loggingChannel"/>
        </integration:interceptors>
    </integration:channel>



    <integration:channel id="requests">
        <integration:interceptors>
            <integration:wire-tap channel="loggingOutChannel"/>
        </integration:interceptors>
    </integration:channel>

    <int-amqp:outbound-channel-adapter channel="requests" amqp-template="amqpTemplate" exchange-name="batch.test.exchange" routing-key="batch.requests"/>
    <int-amqp:inbound-channel-adapter channel="replies" queue-names="batch.replies" connection-factory="connectionFactory" />

    <rabbit:queue name="batch.requests" />
    <rabbit:queue name="batch.replies" />

    <rabbit:direct-exchange name="batch.test.exchange">
        <rabbit:bindings>
            <rabbit:binding queue="batch.requests" key="batch.requests" />
            <rabbit:binding queue="batch.replies" key="batch.replies" />
        </rabbit:bindings>
    </rabbit:direct-exchange>

    <rabbit:admin connection-factory="connectionFactory" />


</beans>